{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf dist && mkdir -p dist && cp -R wwwroot/. dist/ && cp -R videos dist/videos",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "trailingSlash": false
}
